diff --git a/internal/runtime/executor/codex_executor_stream.go b/internal/runtime/executor/codex_executor_stream.go
index 87e4d28..5d00126 100644
--- a/internal/runtime/executor/codex_executor_stream.go
+++ b/internal/runtime/executor/codex_executor_stream.go
@@ -350,6 +350,18 @@ func (e *CodexExecutor) ExecuteStream(ctx context.Context, auth *cliproxyauth.Au
 
 	go func() {
 		defer close(out)
+		terminalSeen := false
+		defer func() {
+			if !terminalSeen && ctx.Err() != nil {
+				// Cancellation is lifecycle evidence, not usage or credential failure.
+				helpers := helps.LogWithRequestID(ctx).WithFields(log.Fields{
+					"event":                "codex.stream.cancelled",
+					"outcome":              "cancelled",
+					"provider_usage_known": false,
+				})
+				helpers.Info("Codex response stream cancelled by caller: event=codex.stream.cancelled outcome=cancelled provider_usage_known=false")
+			}
+		}()
 		defer func() {
 			if errClose := httpResp.Body.Close(); errClose != nil {
 				log.Errorf("codex executor: close response body error: %v", errClose)
@@ -370,6 +382,7 @@ func (e *CodexExecutor) ExecuteStream(ctx context.Context, auth *cliproxyauth.Au
 				translatedLine = append([]byte("data: "), data...)
 				eventType := gjson.GetBytes(data, "type").String()
 				if streamErr, terminalBody, ok := codexTerminalFailureErrWithCooling(data, e.modelLevelCooling()); ok {
+					terminalSeen = true
 					if errClearReplay := clearCodexReasoningReplayOnInvalidSignature(ctx, replayScope, streamErr.StatusCode(), terminalBody); errClearReplay != nil {
 						helps.RecordAPIResponseError(ctx, e.cfg, errClearReplay)
 						reporter.PublishFailure(ctx, errClearReplay)
@@ -404,6 +417,7 @@ func (e *CodexExecutor) ExecuteStream(ctx context.Context, auth *cliproxyauth.Auth
 				case "response.output_item.done":
 					collectCodexOutputItemDone(data, outputItemsByIndex, &outputItemsFallback)
 				case "response.completed", "response.incomplete", "response.done":
+					terminalSeen = true
 					terminalSuccess = true
 					data = normalizeCodexWebsocketCompletion(data)
 					if detail, ok := helps.ParseCodexUsage(data); ok {
diff --git a/internal/runtime/executor/codex_executor_cancel_test.go b/internal/runtime/executor/codex_executor_cancel_test.go
new file mode 100644
index 0000000..4b76a7b
--- /dev/null
+++ b/internal/runtime/executor/codex_executor_cancel_test.go
@@ -0,0 +1,73 @@
+package executor
+
+import (
+	"context"
+	"net/http"
+	"net/http/httptest"
+	"strings"
+	"testing"
+
+	"github.com/router-for-me/CLIProxyAPI/v7/internal/config"
+	"github.com/router-for-me/CLIProxyAPI/v7/internal/logging"
+	cliproxyauth "github.com/router-for-me/CLIProxyAPI/v7/sdk/cliproxy/auth"
+	cliproxyexecutor "github.com/router-for-me/CLIProxyAPI/v7/sdk/cliproxy/executor"
+	sdktranslator "github.com/router-for-me/CLIProxyAPI/v7/sdk/translator"
+	log "github.com/sirupsen/logrus"
+	logtest "github.com/sirupsen/logrus/hooks/test"
+)
+
+func TestCodexStreamCancellationHasDistinctLifecycleEvidence(t *testing.T) {
+	previousHooks := log.StandardLogger().ReplaceHooks(make(log.LevelHooks))
+	defer log.StandardLogger().ReplaceHooks(previousHooks)
+	previousLevel := log.GetLevel()
+	log.SetLevel(log.InfoLevel)
+	defer log.SetLevel(previousLevel)
+	hook := logtest.NewGlobal()
+	server := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
+		w.Header().Set("Content-Type", "text/event-stream")
+		_, _ = w.Write([]byte("data: {\"type\":\"response.created\",\"response\":{\"id\":\"synthetic\",\"status\":\"in_progress\"}}\n\n"))
+		w.(http.Flusher).Flush()
+		<-r.Context().Done()
+	}))
+	defer server.Close()
+	ctx, cancel := context.WithCancel(context.Background())
+	defer cancel()
+	result, err := NewCodexExecutor(&config.Config{}).ExecuteStream(ctx, &cliproxyauth.Auth{Attributes: map[string]string{
+		"base_url": server.URL, "api_key": "synthetic-not-a-secret",
+	}}, cliproxyexecutor.Request{Model: "gpt-6-astra", Payload: []byte(`{"model":"gpt-6-astra","input":"JSON test"}`)}, cliproxyexecutor.Options{
+		SourceFormat: sdktranslator.FromString("openai-response"), Stream: true,
+	})
+	if err != nil {
+		t.Fatal(err)
+	}
+	if _, ok := <-result.Chunks; !ok {
+		t.Fatal("missing response.created")
+	}
+	cancel()
+	for chunk := range result.Chunks {
+		if chunk.Err != nil {
+			t.Fatalf("caller cancellation became upstream failure: %v", chunk.Err)
+		}
+	}
+	count := 0
+	for _, entry := range hook.AllEntries() {
+		if entry.Data["event"] == "codex.stream.cancelled" {
+			count++
+			if entry.Data["outcome"] != "cancelled" || entry.Data["provider_usage_known"] != false {
+				t.Fatalf("wrong cancellation evidence: %v", entry.Data)
+			}
+			formatted, errFormat := (&logging.LogFormatter{}).Format(entry)
+			if errFormat != nil {
+				t.Fatal(errFormat)
+			}
+			for _, field := range []string{"event=codex.stream.cancelled", "outcome=cancelled", "provider_usage_known=false"} {
+				if !strings.Contains(string(formatted), field) {
+					t.Fatalf("persisted lifecycle log missing %s: %s", field, formatted)
+				}
+			}
+		}
+	}
+	if count != 1 {
+		t.Fatalf("cancellation events = %d, want 1", count)
+	}
+}
